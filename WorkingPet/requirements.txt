# WorkingPet 依赖列表
# 安装：pip install -r requirements.txt
#
# 注意：easyocr 会自动拉取 PyTorch（CPU 版约 1-2 GB），首次安装耗时较长。
# keyboard 库需要管理员权限才能全局拦截按键。

PyQt6>=6.4.0
pynput>=1.7.6
psutil>=5.9.0
Pillow>=9.5.0
easyocr>=1.7.0
numpy>=1.24.0
pywin32>=306
keyboard>=0.13.5
pygetwindow>=0.0.9
pyperclip>=1.8.2
